# codex-ralph Progress Log

## Codebase Patterns
- Keep Vitest tests in the top-level `tests/` folder, mirroring the relevant `src/` layout and importing source modules via relative paths.
- Reuse `calculateProductMargin` for per-product gross, net, payout, and margin math when building aggregated calculations.
- Use `generateComparableSimulationOrders` when multiple simulation options need like-for-like order inputs; it clones one seeded order set per option.
- Use `PROMOTION_SCENARIOS` as the source of truth for available commercial scenarios and pass those objects into `calculatePromotionScenarioTotals`.
- Use `createDefaultScenarioSelection`, `toggleScenarioSelection`, and `toSimulationProducts` from `src/lib/simulation-panel.ts` for simulation panel state and product mapping.
- Use `createSimulationScenarioResults` from `src/lib/simulation-panel.ts` for aggregated scenario rows; it reuses one seeded order set per order mode so comparable scenarios share products and quantities.

Started: Thu Jun 18 10:31:40 CEST 2026
---

## 2026-06-18 10:36:58 +02:00 - US-001
- Implemented a pure `src/lib/simulation.ts` calculation engine that aggregates gross revenue, net revenue, payout, supplier cost, margin amount, and margin percent across order lines.
- Scenario discounts are added to product DB discounts with the effective discount capped at 100 percent; payout is calculated from discounted gross revenue paid by the customer.
- Moved existing Vitest files from `src/lib` and `src/lib/server` into `tests/lib` and `tests/lib/server`, then added `tests/lib/simulation.test.ts`.
- Files changed: `src/lib/simulation.ts`, `tests/lib/*.test.ts`, `tests/lib/server/products.test.ts`, `prd.json`, `progress.txt`.
- Quality checks run and results: `npm test -- tests/lib/simulation.test.ts` failed first on the expected gross revenue assertion, then passed; `npm test` passed with 6 files and 18 tests; `npm run check` passed with 0 errors and 0 warnings; `npm run build` passed.
- Browser verification result: not relevant for US-001 because this story adds a pure calculation module and test relocation only.
- **Learnings for future iterations:**
  - Tests now live outside `src`; new tests should be added under `tests/`.
  - `calculateProductMargin` already encodes the product-level revenue, payout, and margin formula, so simulation aggregation should build on it rather than duplicating that math.
  - `rg` is not installed in this environment; use PowerShell enumeration/search commands when needed.
---

## 2026-06-18 10:44:02 +02:00 - US-002
- Implemented seeded random order generation in `src/lib/simulation.ts` for generic, 3x2, and 4x3 simulation modes.
- Added comparable simulation order generation so multiple options receive cloned orders with the same products and quantities.
- Added Vitest coverage for generic order volume and bounds, 3x2 and 4x3 unit counts, deterministic seeds, and like-for-like option inputs.
- Files changed: `src/lib/simulation.ts`, `tests/lib/simulation.test.ts`, `prd.json`, `progress.txt`.
- Quality checks run and results: `npm test -- tests/lib/simulation.test.ts` failed first with missing generator exports, then passed with 6 tests; `npm test` passed with 6 files and 23 tests; `npm run check` passed with 0 errors and 0 warnings; `npm run build` passed.
- Browser verification result: not relevant for US-002 because this story adds pure simulation generation logic and tests only.
- **Learnings for future iterations:**
  - `generateRandomOrders` defaults to 1000 orders and accepts `seed` for deterministic simulations and tests.
  - Bundle order modes use unit totals: 3x2 generates 3 or 6 units; 4x3 generates 4 or 8 units.
  - Use the comparable generator for scenario sets that must share the same products and quantities across options.
---

## 2026-06-18 10:49:46 +02:00 - US-003
- Implemented promotion scenario definitions and order-level scenario total calculation in `src/lib/simulation.ts`.
- Added percentage scenarios, 3x2 and 4x3 cheapest-unit-free logic, and no-KIT handling that matches category `KIT` case-insensitively while applying DB discount plus 20 percent to excluded KIT products.
- Added Vitest coverage for scenario ordering, percentage discount stacking, per-unit bundle freebies, and no-KIT exclusions.
- Files changed: `src/lib/simulation.ts`, `tests/lib/simulation.test.ts`, `prd.json`, `progress.txt`.
- Quality checks run and results: `npm test -- tests/lib/simulation.test.ts` failed first with missing scenario exports, then passed with 11 tests; `npm test` passed with 6 files and 28 tests; `npm run check` passed with 0 errors and 0 warnings; `npm run build` passed.
- Browser verification result: not relevant for US-003 because this story adds pure simulation rules and tests only.
- **Learnings for future iterations:**
  - `PROMOTION_SCENARIOS` includes display names, order modes, percent discounts, bundle group sizes, and no-KIT flags for downstream UI use.
  - `calculatePromotionScenarioTotals` evaluates bundle discounts per order, expanding quantities to units so only the cheapest eligible units become free.
  - No-KIT scenarios exclude only category `KIT` products from bundle counting; those KIT products still remain in the order with DB discount plus 20 percent.
---

## 2026-06-18 11:02:15 +02:00 - US-004
- Implemented a simulation options panel between settings and products on the main workspace.
- Added all promotion scenarios as default-selected checkboxes, scenario activation toggling, a rerun button that advances the simulation launch, and an empty state for workspaces with no saved products.
- Added pure helpers for default scenario selection, selection toggling, and mapping saved product rows into simulation products, with Vitest coverage.
- Files changed: `src/lib/simulation-panel.ts`, `src/routes/SimulationOptionsPanel.svelte`, `src/routes/ProductWorkspace.svelte`, `tests/lib/simulation-panel.test.ts`, `prd.json`, `progress.txt`.
- Quality checks run and results: `npm test -- tests/lib/simulation-panel.test.ts` failed first with missing `src/lib/simulation-panel`, then passed with 3 tests; `npm run check` passed with 0 errors and 0 warnings; `npm test` passed with 7 files and 31 tests; `npm run build` passed.
- Browser verification result: verified against `http://127.0.0.1:5191` with isolated headless Chromium. The panel rendered 9 scenario checkboxes, all were checked by default, unchecking DB/base reduced checked scenarios to 8, saved-product state showed random orders ready, and clicking `Rilancia simulazione` advanced the launch to `#2`. The in-app browser endpoint was unavailable and the MCP browser profile was locked, so verification used a bundled Chromium debugging session.
- **Learnings for future iterations:**
  - `SimulationOptionsPanel.svelte` intentionally owns only option selection and rerun state; aggregated financial result display is still left for a later story.
  - Keep reusable simulation UI state in `src/lib/simulation-panel.ts` so it can be unit-tested without component-test infrastructure.
  - Browser verification may need a fallback isolated Chromium debugging session when the in-app browser endpoint is not registered and the MCP browser profile is locked.
---

## 2026-06-18 11:13:46 +02:00 - US-005
- Implemented aggregated simulation result calculation for selected scenarios, including order count, product count, gross revenue, margin amount, margin percent, and margin tone.
- Rendered desktop table and mobile cards in the simulation panel with Italian EUR and percent formatting plus negative, low, and healthy margin color treatments.
- Passed the current payout percentage into the simulation panel so aggregate margin totals match the saved settings.
- Files changed: `src/lib/simulation-panel.ts`, `src/routes/SimulationOptionsPanel.svelte`, `src/routes/ProductWorkspace.svelte`, `tests/lib/simulation-panel.test.ts`, `prd.json`, `progress.txt`.
- Quality checks run and results: `npm test -- tests/lib/simulation-panel.test.ts` failed first with missing `createSimulationScenarioResults`, then passed with 4 tests; `npm run check` passed with 0 errors and 0 warnings; `npm test` passed with 7 files and 32 tests; `npm run build` passed.
- Browser verification result: verified against `http://127.0.0.1:5192` with isolated headless Chromium after the in-app browser endpoint was unavailable and the MCP browser profile was locked. Desktop rendered 9 scenario rows with Italian currency and percent formatting. Mobile rendered 9 cards, hid the desktop table, and had no horizontal overflow at 390px width. Unchecking `DB/base` reduced rows to 8 and removed its result; clicking `Rilancia simulazione` advanced the launch to `#2` and changed generated totals.
- **Learnings for future iterations:**
  - Aggregated result display should stay in `SimulationOptionsPanel.svelte`, while result construction belongs in `src/lib/simulation-panel.ts` for unit coverage.
  - `createSimulationScenarioResults` accepts an optional small `orderCount` for deterministic focused tests while the UI uses the generator default of 1000 orders.
  - Scenario result views need the current `payoutPercent`; route-level wiring should pass it anywhere aggregate margins are displayed.
---

## 2026-06-18 11:20:00 +02:00 - US-006
- Updated the Italian and English README feature lists and usage notes to describe order and promotion simulations.
- Documented selectable scenarios, rerun behavior, generic order assumptions, and the required 3x2/4x3 simulated product counts.
- Files changed: `README.md`, `prd.json`, `progress.txt`.
- Quality checks run and results: baseline `npm test` passed with 7 files and 32 tests; baseline `npm run check` passed with 0 errors and 0 warnings; final `npm test` passed with 7 files and 32 tests; final `npm run check` passed with 0 errors and 0 warnings; final `npm run build` passed.
- Browser verification result: not relevant for US-006 because this story changes README documentation only.
- **Learnings for future iterations:**
  - The project currently has one bilingual README; feature and usage documentation should be updated in both `Italiano` and `English` sections.
---
